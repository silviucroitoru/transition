/**
 * Branding: logo and colors.
 * - Logo: edit logo.src (path or URL), logo.alt, logo.link.
 * - Colors: edit CSS variables in src/assets/base.css (:root).
 */

export const brand = {
  appName: 'Transition',
  logo: {
    src: '/an_logo.svg',
    alt: 'Alexandra Negulescu',
    link: '/',
  },
};
